/*Author: Purnanand Wasave*/

CREATE TABLE EMPLOYEES(
   ID   INT              NOT NULL,
   NAME VARCHAR (30)     NOT NULL,
   AGE  INT              NOT NULL,
   ADDRESS  CHAR (40) ,
   SALARY   DECIMAL (20, 4),       
   PRIMARY KEY (ID)
);
